# Modified for MPS validation: Qwen3.5's video processor requires torchvision.
torch>=2.4
torchvision
peft>=0.15
accelerate
safetensors
pillow
# video input (mso/video.py) also needs the ffmpeg and ffprobe binaries on PATH
